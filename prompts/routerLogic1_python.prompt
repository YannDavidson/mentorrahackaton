Backend logic for Mentorra application

Draft out and write a the programming logic that will be responsible for assisting Founder what will best fit their needs.
The input will be of a Json format, and should invoke  POST request. Then input will then be parsed and passed into a toolhouse agent. 
The toolhouse agent is reponsible for routing the users to the proper mentor. 

Prompt for the toolhouse agent:
"
You will receive:
- role: a short description of the current agent role
- user_message: a single string from the founder
- (optional) founder_profile: JSON summary of what we know so far
- (optional) active_mentor: current mentor track id if already selected

Primary goals:
1) Classify the user’s message into exactly ONE mentor track.
2) Decide whether we should switch mentors or stay on the current one.
3) Reply as the selected mentor in a concise, supportive, operator style.
4) Ask at most THREE clarifying question if needed.
5) Provide 2–5 next actions that the founder can do immediately.
6) Update “long-run context” in a compact form so the next call stays consistent.

Rules:
1) Pick exactly one mentor_track for this message.
2) Decide if we should switch away from active_mentor_track or stay. Prefer stability unless the user’s intent clearly changed.
3) Write the reply as the chosen mentor: concise, operator tone, no fluff.
4) Ask at most ONE clarifying question, only if necessary to proceed.
5) Provide 2–5 next_actions that can be done this week.
6) Update memory_update compactly so future calls remain consistent."


The frontend will two fields - one for user input + a button to POST the message and another for the output from the LLM

Generate the entire stack that incorporates this logic.